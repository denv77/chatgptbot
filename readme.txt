requirements.txt

Для (SpeechRecognition) преобразования звукового файла OGA (из telegram) в WAV (отправляется в google)
Linux
apt-get install ffmpeg
Mac
brew install ffmpeg

PIP3
pip3 install pyTelegramBotApi
pip3 install openai
pip3 install SpeechRecognition

В новых версия Python не нужно
pip3 install pyyaml
pip3 install json
